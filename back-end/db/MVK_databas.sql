CREATE TABLE weather_data
  (Timestamp DATETIME PRIMARY KEY, Teperature DECIMAL, Overcast DECIMAL, Wind DECIMAL, Consumption DECIMAL);

CREATE TABLE weather_forecast
  (Timestamp DATETIME PRIMARY KEY, Teperature DECIMAL, Overcast DECIMAL, Wind DECIMAL)

CREATE TABLE 
